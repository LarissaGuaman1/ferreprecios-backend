// Lista única de sectores válidos de Quito. La usan el modelo
// Ferreteria (para validar) y, más adelante, el filtro del buscador.
const SECTORES = ['Norte', 'Sur', 'Centro', 'Valles'];

module.exports = SECTORES;
